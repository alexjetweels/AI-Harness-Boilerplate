---
description: Implement the planned todo app tasks.
allowed-tools: Read, Write, Edit, Bash
argument-hint: "<optional task subset>"
---

Implement the tasks in `docs/sdlc/current/05-tasks.md`.

Rules:

- Read the intake, requirements, architecture, plan, and tasks before editing.
- Keep changes scoped to this todo app.
- Update `docs/sdlc/current/06-implementation.md` with changed files, decisions, and commands run.
- Run relevant checks before claiming completion.
- Stop and update architecture notes if implementation exposes a design conflict.

