# Core Rules

- Keep this demo app dependency-free unless explicitly required.
- Preserve CLI behavior and JSON-file persistence.
- Update tests and acceptance checks when behavior changes.
- Store SDLC artifacts under `docs/sdlc/current/`.

