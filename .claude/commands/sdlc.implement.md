---
description: Implement tasks from the current SDLC plan.
allowed-tools: Read, Write, Edit, Bash
argument-hint: "<optional task subset>"
---

Implement the tasks in `docs/sdlc/current/05-tasks.md`.

Rules:

- Read intake, requirements, architecture, plan, and tasks before editing.
- Keep changes scoped to the planned work.
- Update `docs/sdlc/current/06-implementation.md` with changed files, decisions made during implementation, and commands run.
- Do not mark verification as passed unless commands were run successfully.
- If implementation exposes a design conflict, stop and update the architecture notes before continuing.

