---
description: Break the plan into executable tasks with verification.
allowed-tools: Read, Write, Edit, Bash
argument-hint: "<optional task constraints>"
---

Create or update `docs/sdlc/current/05-tasks.md`.

Use checkboxes. Each task must include:

- The intended change.
- The target files or modules.
- The verification command or inspection step.
- The requirement IDs it supports when available.

Do not implement the tasks in this command.

