Build a command-line todo application. Users can add a task, list all tasks,
mark a task complete, and delete a task. Tasks persist between runs in a local
JSON file. Each task has an id, a title, and a done flag.
